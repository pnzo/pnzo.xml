<Window x:Class="xml.task.NewCommandWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:xml.task.Windows"
        mc:Ignorable="d"
        Title="Новая команда" Height="300" Width="500"
        >
    <Grid>
        <ListBox Name="TemplateListBox"
            ItemsSource="{Binding Path=Templates}" ScrollViewer.HorizontalScrollBarVisibility="Disabled" MouseDown="ListBox_MouseDown" MouseDoubleClick="TemplateListBox_MouseDoubleClick">
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Height" Value="75" />
                </Style>
            </ListBox.ItemContainerStyle>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Margin="5">
                        <TextBlock FontSize="16" Text="{Binding Path=Name}" HorizontalAlignment="Left" />
                        <TextBlock FontSize="12" Text="{Binding Path=Description}" HorizontalAlignment="Left" TextWrapping="Wrap" />
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</Window>
